name = "veer"
#to print length-
p = len(name)
print(p)

#to check
print(name.endswith("er"))
print(name.startswith("abh"))
print(name.capitalize()) #only capitalizes the first character


#strip() — remove spaces from ends
#replace() — replace words/letters
# split() — split into list
# find() — find position(-1 if not found)
# count() — count occurrences
# startswith(), endswith()
# isdigit() — check number
# join() — join list into string